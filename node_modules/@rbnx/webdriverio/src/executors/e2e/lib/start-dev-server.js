"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.startDevServer = void 0;
const devkit_1 = require("@nx/devkit");
async function startDevServer(options, context) {
    const { baseUrl, devServerTarget, skipServe } = options;
    if (!devServerTarget || skipServe) {
        return baseUrl;
    }
    const target = (0, devkit_1.parseTargetString)(devServerTarget, context.projectGraph);
    for await (const output of await (0, devkit_1.runExecutor)(target, {}, context)) {
        if (!output.success) {
            throw new Error(`Could not start dev server for ${target.project} project`);
        }
        return baseUrl || output.baseUrl;
    }
}
exports.startDevServer = startDevServer;
//# sourceMappingURL=start-dev-server.js.map
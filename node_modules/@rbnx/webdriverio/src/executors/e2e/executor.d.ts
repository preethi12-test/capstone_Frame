import { ExecutorContext } from '@nx/devkit';
import type { Schema } from './schema';
export default function runExecutor(options: Schema, context: ExecutorContext): Promise<{
    success: boolean;
}>;
